# Set mod key (Mod1=<Alt>, Mod4=<Super>)
exec --no-startup-id xmodmap -e "keycode 135 = Super_L"
set $mod Mod4

# set keyboard layouts and set alt+shift to be layout toggle
exec --no-startup-id "setxkbmap -layout us,ru"
exec --no-startup-id "setxkbmap -option 'grp:alt_shift_toggle'"

# set keyboard repeat delay and repeat interval
exec --no-startup-id "xset r rate 200 40"

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
font xft:Comfortaa

# Use Mouse+$mod to drag floating windows
floating_modifier $mod

bindsym $mod+F12 exec "shortcuts | rofi -dmenu -p 'Search a shortcut: ' -theme .local/share/rofi/themes/run.rasi -theme-str 'window {width: 50%;}' -i"
# show this shortcuts finder

bindsym $mod+Return exec terminal
# open terminal

bindsym $mod+c kill
# kill focused window

bindsym $mod+Ctrl+l exec --no-startup-id betterlockscreen -l blur
# lock screen

bindsym $mod+Escape exec rofi -lines 8 -padding 18 -location 0 -show drun -sidebar-mode -columns 6 -font 'Comfortaa 12' -theme ./.local/share/rofi/themes/sidetab-adapta.rasi
# rofi fullscreen app launcher
bindsym $mod+d exec --no-startup-id rofi -modi "combi#files:/home/obey/.bins/dmenufiles#drun#run#window#ssh" -show combi -combi-modi "drun#run#window#ssh" -theme ./.local/share/rofi/themes/run.rasi -run-shell-command 'terminal -t "terminal #widget" -e zsh -ic "echo {cmd} && {cmd} && read"'
# rofi center app launcher
# Screen @brightness controls
bindsym XF86MonBrightnessUp     exec xbacklight -inc 10
# brightness +10
bindsym XF86MonBrightnessDown   exec xbacklight -dec 10
# brightness -10
bindsym XF86_AudioLowerVolume   exec "pulseaudio-ctl down"
# volume down
bindsym XF86_AudioMute          exec "pulseaudio-ctl mute; pulseaudio-ctl mute-input"
# volume mute
bindsym XF86_AudioRaiseVolume   exec "pulseaudio-ctl up"
# volume up
bindsym $mod+F7                 exec "terminal -t 'pulsemixer #mini #widget' -e 'pulsemixer'"
# pulsemixer widget
bindsym XF86_AudioPlay          exec "playerctl play-pause"
# player play/pause
bindsym XF86_AudioPrev          exec "playerctl prev"
# player prev
bindsym XF86_AudioNext          exec "playerctl next"
# player next
bindsym $mod+F11                exec "terminal -t 'mocp #widget' -e 'mocp'"
# mocp player

# @TEMPERATURE
# Screen color temperature control
bindsym $mod+F4 exec sct 6600
# set screen temperature to 6600
bindsym $mod+F3 exec sct 4600
# set screen temperature to 4600

bindsym $mod+F5 exec create-android-display.sh -p -s -l
# Starts remote second monitor

# @SHORTCUTS APPS
# Start Applications
bindsym $mod+F1 exec qalculate-gtk
# open calculator
bindsym $mod+F2 exec networkmanager-dmenu
# open rofi network manager
bindsym $mod+w exec $BROWSER
# open browser
bindsym $mod+e exec terminal -e ranger
# open CLI file browser
bindsym $mod+Shift+e exec pcmanfm
# open GUI file browser
bindsym $mod+o exec dmenuhandler
# open rofi link handler
bindsym $mod+grave exec dmenuunicode
# open rofi emoji selector
bindsym $mod+t exec "terminal -t 'terminal #minifloat' -e zsh -is eval rxfetch"
# open small floating terminal
bindsym $mod+Ctrl+x --release exec --no-startup-id xkill
# select and kill
bindsym $mod+Prior exec "terminal -t 'terminal config' -e zsh -c 'dotfiles -c edit'"
# open config
bindsym $mod+Next exec "doas terminal -t 'bmenu #widget' -e bmenu"
# launch ToolBox
bindsym Ctrl+Escape exec "terminal -t 'terminal top #widget' -e bpytop -b 'cpu proc'"
# open system monitor
bindsym $mod+Home floating enable, sticky enable, border pixel 0, move position 1398 35, resize set 512 288
# make window sticky widget upper right
bindsym $mod+End floating enable, sticky enable, border pixel 0, move position 1398 782, resize set 512 288
# make window sticky widget lower right

bindsym Print exec --no-startup-id "i3-scrot -dc"
# screenshot whole screen to clipboard
bindsym $mod+Print --release exec --no-startup-id "i3-scrot -wc"
# screenshot active window to clipboard
bindsym $mod+Shift+Print --release exec --no-startup-id "i3-scrot -sc"
# screenshot selection to clipboard
bindsym $mod+Ctrl+Print --release exec --no-startup-id xclip -selection clipboard -t image/png -o >> "$(date +'/home/obey/Pictures/Screenshots/%Y-%m-%d-%I_%M_%S_%p').png" & notify-send "screenshot saved"
# save last screenshot to $HOME/Pictures/Screenshots/...

focus_follows_mouse yes

bindsym $mod+h focus left
# move focus left
bindsym $mod+k focus down
# move focus down
bindsym $mod+j focus up
# move focus up
bindsym $mod+l focus right
# move focus right

bindsym $mod+Left  focus left
# move focus left
bindsym $mod+Down  focus down
# move focus down
bindsym $mod+Up    focus up
# move focus up
bindsym $mod+Right focus right
# move focus right

bindsym $mod+Shift+h move left  60px
# move window left (60px if floating)
bindsym $mod+Shift+k move down  60px
# move window down (60px if floating)
bindsym $mod+Shift+j move up    60px
# move window up (60px if floating)
bindsym $mod+Shift+l move right 60px
# move window right (60px if floating)

bindsym $mod+Shift+Left  move left  20px
# move window left (20px if floating)
bindsym $mod+Shift+Down  move down  20px
# move window down (20px if floating)
bindsym $mod+Shift+Up    move up    20px
# move window up (20px if floating)
bindsym $mod+Shift+Right move right 20px
# move window right (20px if floating)

# workspace back and forth (with/without active container)
workspace_auto_back_and_forth yes
bindsym $mod+b workspace back_and_forth
# switch to previous workspace

# split orientation
bindsym $mod+Ctrl+h split h;exec notify-send 'tile horizontally'
# set tiling to horizontal
bindsym $mod+Ctrl+v split v;exec notify-send 'tile vertically'
# set tiling to vertical
bindsym $mod+Tab split toggle
# toggle tiling mode

bindsym $mod+f fullscreen toggle
# toggle fullscreen

bindsym $mod+q layout toggle
# toggle layout (stacked/tabbed)

bindsym $mod+Shift+space floating toggle
# toggle floating

bindsym $mod+space focus mode_toggle
# switch to floating/tiling windows

bindsym $mod+s sticky toggle
# toggle stickiness

# focus the parent container
bindsym $mod+a focus parent
# focus parent container (select all windows)

# move the currently focused window to the scratchpad
bindsym $mod+Shift+minus move scratchpad
# move window to scratchpad

# Show the next scratchpad window or hide the focused scratchpad window.
# If there are multiple scratchpad windows, this command cycles through them.
bindsym $mod+minus scratchpad show
# go through scratchpad windows

# navigate workspaces next / previous
bindsym Mod1+Tab workspace next
# next workspace
bindsym Mod1+Shift+Tab workspace prev
# previous workspace

# Workspace names
# to display names or symbols instead of plain workspace numbers you can use
# something like: set $ws1 1:mail
#                 set $ws2 2:
set $ws1 1:term
set $ws2 "2:qute"
set $ws3 "3:edit"
set $ws4 4:social
set $ws5 "5:game"
set $ws6 "6:gimp"
set $ws7 "7:media"
set $ws8 "8:misc"
set $ws9 "9:opt"

# switch to workspace
bindsym $mod+1 workspace $ws1
# switch to workspace 1
bindsym $mod+2 workspace $ws2
# switch to workspace 2
bindsym $mod+3 workspace $ws3
# switch to workspace 3
bindsym $mod+4 workspace $ws4
# switch to workspace 4
bindsym $mod+5 workspace $ws5
# switch to workspace 5
bindsym $mod+6 workspace $ws6
# switch to workspace 6
bindsym $mod+7 workspace $ws7
# switch to workspace 7
bindsym $mod+8 workspace $ws8
# switch to workspace 8
bindsym $mod+9 workspace $ws9
# switch to workspace 9

# Move focused container to workspace
bindsym $mod+Ctrl+1 move container to workspace $ws1
# move window to workspace 1
bindsym $mod+Ctrl+2 move container to workspace $ws2
# move window to workspace 2
bindsym $mod+Ctrl+3 move container to workspace $ws3
# move window to workspace 3
bindsym $mod+Ctrl+4 move container to workspace $ws4
# move window to workspace 4
bindsym $mod+Ctrl+5 move container to workspace $ws5
# move window to workspace 5
bindsym $mod+Ctrl+6 move container to workspace $ws6
# move window to workspace 6
bindsym $mod+Ctrl+7 move container to workspace $ws7
# move window to workspace 7
bindsym $mod+Ctrl+8 move container to workspace $ws8
# move window to workspace 8
bindsym $mod+Ctrl+9 move container to workspace $ws9
# move window to workspace 9

# Move to workspace with focused container
bindsym $mod+Shift+1 move container to workspace $ws1; workspace $ws1
# move window and focus to workspace 1
bindsym $mod+Shift+2 move container to workspace $ws2; workspace $ws2
# move window and focus to workspace 2
bindsym $mod+Shift+3 move container to workspace $ws3; workspace $ws3
# move window and focus to workspace 3
bindsym $mod+Shift+4 move container to workspace $ws4; workspace $ws4
# move window and focus to workspace 4
bindsym $mod+Shift+5 move container to workspace $ws5; workspace $ws5
# move window and focus to workspace 5
bindsym $mod+Shift+6 move container to workspace $ws6; workspace $ws6
# move window and focus to workspace 6
bindsym $mod+Shift+7 move container to workspace $ws7; workspace $ws7
# move window and focus to workspace 7
bindsym $mod+Shift+8 move container to workspace $ws8; workspace $ws8
# move window and focus to workspace 8
bindsym $mod+Shift+9 move container to workspace $ws9; workspace $ws9
# move window and focus to workspace 9

# Open applications on specific workspaces
assign [title="(?i)qutebrowser(?i)"] $ws2
assign [title="(?i)Code - OSS(?i)"] $ws3
assign [title="(?i)Sublime(?i)"] $ws3
assign [title="(?i)Discord(?i)"] $ws4
assign [title="(?i)Steam(?i)"] $ws5
assign [title="(?i)PlayOnLinux(?i)"] $ws5
assign [title="(?i)Lutris(?i)"] $ws5
assign [title="(?i)image manipulation(?i)"] $ws6
assign [title="(?i)vlc(?i)"] $ws7

# Open specific applications in floating mode
for_window [title="alsamixer"] floating enable border normal
for_window [title="(?i)#float(?i)"] floating enable, border pixel 0
for_window [title="(?i)#widget(?i)"] floating enable, border pixel 0, resize set 780 0, move position 1130 35
for_window [title="(?i)#mini(?i)"] floating enable, border pixel 0, resize set 0 260
for_window [title="(?i)#minifloat(?i)"] floating enable, border pixel 0, resize set 580 400, move position center
for_window [title="(?i)terminal #widget date(?i)"] floating enable, border pixel 0, resize set 740 835, move position 590 35
for_window [title="(?i)Telegram(?i)"] floating enable, sticky enable, border pixel 0, resize set 475 600, move position 10 35
for_window [class="Nitrogen"] floating enable sticky enable border normal
for_window [class="Pamac-manager"] floating enable
for_window [class="Pavucontrol"] floating enable
for_window [class="(?i)clipit(?i)"] floating enable border pixel 0
for_window [class="(?i)Qalcula(?i)"] floating enable, border pixel 0, resize set 780 260, move position 1130 35
for_window [title="cvlc(?i)"] floating enable, sticky enable
for_window [title="mpv(?i)"] floating enable, sticky enable, border pixel 0, move position 320 180, resize set 1280 720
for_window [title="(?i)Network Connections(?i)"] floating enable border pixel 0
for_window [title="(?i)New pallete - Gpick(?i)"] floating enable border pixel 0

# switch to workspace with urgent window automatically
for_window [urgent=latest] focus

# reload the configuration file
bindsym $mod+Shift+c reload
# reload i3 config

# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# restart i3

# @SYSTEMMODE
bindsym $mod+Shift+q exec /home/obey/.bins/powerdmenu
# rofi powermenu

# @Resize window (you can also use the mouse for that)
bindsym $mod+r mode "resize"
# mode resize
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode
        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym a resize shrink width 1 px or 1 ppt
        bindsym s resize grow height 1 px or 1 ppt
        bindsym w resize shrink height 1 px or 1 ppt
        bindsym d resize grow width 1 px or 1 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # exit resize mode: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}


# @Autostart applications
exec --no-startup-id xfce4-power-manager
exec --no-startup-id clipit
exec --no-startup-id kdeconnect-indicator
exec_always --no-startup-id ff-theme-util
exec_always --no-startup-id fix_xcursor
exec_always --no-startup-id nitrogen --restore
exec_always --no-startup-id killall picom; picom -b --experimental-backends --config '/home/obey/.config/picom.conf'
exec_always "killall dunst; notify-send --urgency=critical 'Restarted Dunst'"

bindsym $mod+backslash exec dunstctl close
# close top-most notification
bindsym $mod+Shift+backslash exec dunstctl history-pop
# show last notification from history
bindsym $mod+Ctrl+backslash exec dunstctl context
# rofi context menu for displayed notifications


# exec --no-startup-id nm-applet

# Color palette used for the terminal ( ~/.Xresources file )
# Colors are gathered based on the documentation:
# https://i3wm.org/docs/userguide.html#xresources
# Change the variable name at the place you want to match the color
# of your terminal like this:
# [example]
# If you want your bar to have the same background color as your
# terminal background change the line 362 from:
# background #14191D
# to:
# background $term_background
# Same logic applied to everything else.
set_from_resource $background background
set_from_resource $foreground foreground
set_from_resource $blackD     color0
set_from_resource $redD       color1
set_from_resource $greenD     color2
set_from_resource $yellowD    color3
set_from_resource $blueD      color4
set_from_resource $magentaD   color5
set_from_resource $pinkD      color6
set_from_resource $whiteD     color7
set_from_resource $blackL     color8
set_from_resource $redL       color9
set_from_resource $greenL     color10
set_from_resource $yellowL    color11
set_from_resource $blueL      color12
set_from_resource $magentaL   color13
set_from_resource $pinkL      color14
set_from_resource $whiteL     color15

# @BAR
exec_always --no-startup-id /home/obey/.config/polybar/launch.sh &

# @THEME COLORS
# class                 border    backgr.   text      indic.    child_border
client.focused          $yellowD  $blackD   $whiteD   $redD     $yellowD
client.focused_inactive $blackL   $blackD   $whiteD   $pinkd    $blackL
client.unfocused        $blackL   $blackD   $whiteD   $pinkd    $blackL
client.urgent           $redD     $blackD   $whiteD   $pinkd    $pinkd
client.placeholder      #000000   #0c0c0c   $whiteD   #000000   #0c0c0c

client.background       $whiteL


#############################
### settings for @borders:###
#############################

# Configure border style <normal|1pixel|pixel xx|none|pixel>
default_border pixel 0
default_floating_border pixel 0

# Hide borders
hide_edge_borders none

# change borders

set $mode_borders (Borders) numbers 0-9: set border width to n*2 pixels, (N)ormal, (n)one
bindsym $mod+Shift+b mode "$mode_borders"
# mode borders

mode "$mode_borders" {
        bindsym 0        border none
        bindsym 1        border pixel 2
        bindsym 2        border pixel 4
        bindsym 3        border pixel 6
        bindsym 4        border pixel 8
        bindsym 5        border pixel 10
        bindsym 6        border pixel 12
        bindsym 7        border pixel 14
        bindsym 8        border pixel 16
        bindsym 9        border pixel 18
        bindsym n        border none
        bindsym Shift+n        border normal
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

#############################
### settings for i3-gaps: ###
#############################

# Set inner/outer gaps
# @GAPS
gaps inner 0
gaps outer 0

# Smart gaps (gaps used if only more than one container on the workspace)
smart_gaps inverse_outer
smart_gaps on

# Smart borders (draw borders around container only if it is not the only container on this workspace)
# on|no_gaps (on=always activate and no_gaps=only activate if the gap size to the edge of the screen is 0)
smart_borders on

# Press $mod+Shift+g to enter the gap mode. Choose o or i for modifying outer/inner gaps. Press one of + / - (in-/decrement for current workspace) or 0 (remove gaps for current workspace). If you also press Shift with these keys, the change will be global for all workspaces.
set $mode_gaps Gaps: (o)uter, (i)nner, (e)xpand, (f)ill
set $mode_gaps_outer Outer Gaps: +|-|0 (local), Shift + +|-|0 (global)
set $mode_gaps_inner Inner Gaps: +|-|0 (local), Shift + +|-|0 (global)
bindsym $mod+Shift+g mode "$mode_gaps"
# mode gaps

mode "$mode_gaps" {
        bindsym o      mode "$mode_gaps_outer"
        bindsym i      mode "$mode_gaps_inner"
        bindsym e      gaps inner all set 50; gaps outer all set 0
        bindsym f      gaps inner all set 0;  gaps outer all set 0
        bindsym Return mode "default"
        bindsym Escape mode "default"
}
mode "$mode_gaps_inner" {
        bindsym plus  gaps inner current plus 5
        bindsym minus gaps inner current minus 5
        bindsym 0     gaps inner current set 0

        bindsym Shift+plus  gaps inner all plus 5
        bindsym Shift+minus gaps inner all minus 5
        bindsym Shift+0     gaps inner all set 0

        bindsym Return mode "default"
        bindsym Escape mode "default"
}
mode "$mode_gaps_outer" {
        bindsym plus  gaps outer current plus 5
        bindsym minus gaps outer current minus 5
        bindsym 0     gaps outer current set 0

        bindsym Shift+plus  gaps outer all plus 5
        bindsym Shift+minus gaps outer all minus 5
        bindsym Shift+0     gaps outer all set 0

        bindsym Return mode "default"
        bindsym Escape mode "default"
}
